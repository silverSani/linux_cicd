name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed
    
jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Delete Docker Image and Containers
        run: docker rm -f silversani
      - name: Delete Docker Image and Containers
        run: Pull Docker Image from Docker hub
      - name: Execute Docker Container
        run: sudo docker run -d -p 80:3000 -- name silversani silversani/incheon:latest